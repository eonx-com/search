input {
  pipeline {
    address => output_es
  }
}

output {
  elasticsearch {
    hosts => ["${ELASTICSEARCH_HOST}"]
    index => "%{[@metadata][es][_index]}"
    action => "%{[@metadata][es][_action]}"
    doc_as_upsert => true
    document_id => "%{[@metadata][es][_id]}"
  }
}
